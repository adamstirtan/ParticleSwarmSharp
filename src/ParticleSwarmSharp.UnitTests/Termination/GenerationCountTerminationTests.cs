using ParticleSwarmSharp.Termination;

using Moq;

namespace ParticleSwarmSharp.UnitTests.Termination
{
    [TestClass]
    public class GenerationCountTerminationTests
    {
        [TestMethod]
        public void TerminatesAfter100GenerationsTest()
        {
            //const int generations = 10;

            //Mock<IParticleSwarm> mockSwarm = new();

            //GenerationCountTermination termination = new(generations);

            //for (int i = 0; i < generations; i++)
            //{
            //    Assert.IsFalse(termination.HasReached(mockSwarm.Object));
            //}

            //Assert.IsTrue(termination.HasReached(mockSwarm.Object));
        }
    }
}